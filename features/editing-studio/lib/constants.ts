/** Base path for all Editing Studio routes (App Router). */
export const EDITING_STUDIO_BASE_PATH = "/editing-studio";

/** Local JSON store (under repo root; gitignored via /data/local/*). */
export const EDITING_STUDIO_STORE_FILE = "data/local/editing-studio-store.json";

export const EDITING_STUDIO_STORE_VERSION = 1 as const;
