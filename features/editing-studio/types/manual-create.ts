import type { ManualProjectCreateInput } from "@/features/editing-studio/validators/editing-studio-schemas";

/** Request body for manual project creation (validated with {@link manualProjectCreateSchema}). */
export type ManualProjectCreateRequest = ManualProjectCreateInput;
